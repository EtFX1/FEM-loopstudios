/* the size where the mobile image maxes out, so the desktop image has to be used here even though this is not desktop size yet */
@media (min-width: 992px) {
    header {
        background-image: url("../images/desktop/image-hero.jpg");
    }

    header > #h1-cont {
        transform: unset;
        min-width: 413px;
        max-width: unset;
        width: 50%;
    }

    h1 {
        font-size: clamp(2.578rem, 4.103vw + 0.655rem, 3.75rem);
    }

    /*images change on desktop size*/
    .img-cont:nth-child(1) {
        background-image: url("../images/desktop/image-deep-earth.jpg");
    }

    /* !header (desktop navbar shown) */
    #hamburger-btn,
    #close-nav-btn {
        display: none;
    }

    #logo-and-close-btn > .logo {
        display: none;
    }

    nav {
        /* border: 2px solid white; */
        padding: 0;
        margin-right: 40px;
        position: relative; /*so that container with nav ;links can have position absolute and be moved down*/
        width: fit-content;
        background-color: unset;
        display: block;
    }

    /* shows the navbar */
    .hide-nav {
        display: unset;
    }

    #nav-links-cont {
        margin-top: 15px;
        flex-direction: row;
        column-gap: 20px;
    }

    #nav-links-cont a {
        font-size: 1.25rem;
        top: 0;
        display: inline;
        text-transform: unset;
        font-weight: 400;
    }
}
